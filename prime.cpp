#include <iostream>

using namespace std;

int main()
{
    int a, factor = 2, bigFactor=1;
    cout << "Prime Numbers To:" << endl;
    cin >> a;
    for
    while (factor <= sml){
        while (a % factor == 0){
            a = a/factor;
            sml = sml/factor;
            bigFactor = bigFactor*factor;
        }
        factor++;
    }
}
